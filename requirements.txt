# music-organiser — Python dependencies
# Install: pip install -r requirements.txt

# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Audio tag reading/writing
mutagen>=1.47.0

# Metadata providers
requests>=2.28.0
requests-ratelimiter>=0.4.0
discogs-client>=2.3.0
musicbrainzngs>=0.7.1
pyacoustid>=1.3.0
# fingerprint_kit.py's comparison path (decode_fingerprint + bit similarity)
# needs the actual libchromaprint shared library, not a pip package — on
# Linux install it via the system package manager (e.g. apt install
# libchromaprint1) if fingerprint_kit.AVAILABLE comes up False; on Windows
# a prebuilt copy ships in vendor/chromaprint-windows-x64/ since no
# standalone redistributable exists upstream (see that folder's BUILD.md).
# The Python ctypes wrapper itself is vendored too, NOT pip-installed —
# see vendor/pyacoustid-chromaprint/README.md for why.

# Image processing (cover art, spectrograms)
Pillow>=9.0.0

# Fake-FLAC detection + spectrogram rendering (FFT spectral analysis)
numpy>=1.24.0
soundfile>=0.12.0

# Config (Python 3.10 has no built-in tomllib)
tomli>=2.0.0

# Data validation (FastAPI dep, pin for compat)
pydantic>=2.0.0
